# Android unit — MOVED to Sovereign SOMA Field

**Do not develop Android on the Mac Studio DOJO tree.**  
Moving this work into SOMA exists so **local FIELD (`/Users/field`) can reclaim disk**.

## New home (GitHub-accurate)

```text
nexus-infinity/Field-NixOS-SOMA
  suite/android/apps/… or suite/android/lab/…
```

Residence: **teal SOMA** · Mandela: DOJO · SOMA · PULSE (lines open)  
Build: distance-built from Mac Studio · do not require NixOS login as daily IDE  
Local FIELD desk: **`/Users/field/`** (not `/Users/jbear/`)

## Space law

| Mode | Disk effect |
|------|-------------|
| Default sync (`pointer`) | SOMA clone + old tree both present — **does not free space** |
| `MOVE_MODE=replace` | Old FIELD tree → backup + stub — **reclaims Studio DOJO space** |

Clear `HOLD.DojoDiskReclaimPending` only with a `RECLAIM_*` receipt after replace.

## Pointer law

- Green DOJO / local FIELD path = stub only after replace
- Teal SOMA suite = durable code home
- Slate PULSE = treaty/receipt only — not the garage

See: `suite/android/README.md` · `suite/android/AGENTS.md` · FIELD Mandela Highest Residence V0
